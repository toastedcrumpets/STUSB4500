# STUSB4500

A sigrok/pulseview decoder for the STUSB4500 I2C interface.

Note, at this time the decoder only identifies what registers are being written/read. It does not parse the results.

# Installation

Copy the `pd.py` and `__init__.py` files to a folder called STUSB4500 in your decoders directory.
